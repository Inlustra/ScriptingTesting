buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath group: 'org.zeroturnaround', name: 'gradle-jrebel-plugin', version: '1.1.2'
    }
}

dependencies {
    compile project(":LightLib")
}
apply plugin: 'rebel'

jar.dependsOn(generateRebel)
